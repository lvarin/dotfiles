- name: Install tmux
  package: name=tmux state=present
  become_user: root
  become: true
  become_method: sudo

- name: Copy .tmux.conf
  copy:
    src: "tmux.conf"
    dest: "~/.tmux.conf"
  become_user: root
  become: true
  become_method: sudo


